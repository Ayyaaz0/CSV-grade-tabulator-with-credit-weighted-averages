# CSV Grade Tabulator (C)

A command-line tool to calculate module and credit-weighted averages from CSV files.
Supports weighted assessments and grouped (best-of-N) components.

## Build
```bash
cd gradecalc
make
